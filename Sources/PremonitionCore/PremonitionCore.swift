/// Namespace for the headless Premonition product core.
///
/// S0 intentionally contains no product behaviour. Safety-critical gate,
/// resolver, executor, validation and state-machine work begins in S1.
public enum PremonitionCore {
    public static let minimumMacOSVersion = "14.0"
}
